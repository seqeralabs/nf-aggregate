# render_ic_report

Renders `report_data_ic.json` into `intelligent_compute_report.html` using the shared `benchmark_report_render.render_report_from_json` with the IC template `bin/benchmark_report_ic_template.html`. Entrypoint `render_ic_report.py` (top-level `bin/`).
