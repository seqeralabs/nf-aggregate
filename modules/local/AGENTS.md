# modules/local/ — Nextflow Processes

Each module has its own `AGENTS.md` with full details.

| Module              | Status     | Purpose                                       |
| ------------------- | ---------- | --------------------------------------------- |
| `benchmark_report/` | **active** | Python + DuckDB + eCharts report              |
| `extract_tarball/`  | active     | Extract run-data tarballs for external runs   |
