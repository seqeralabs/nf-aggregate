# modules/local/ — Nextflow Processes

Each module has its own `AGENTS.md` with full details.

| Module | Status | Purpose |
|---|---|---|
| `benchmark_report_v2/` | **active** | Python + DuckDB + eCharts report |
| `seqera_runs_dump/` | active | Tower CLI `tw runs dump`, metadata enrichment |
| `plot_run_gantt/` | active | Fusion-only Gantt chart (plotly) |
